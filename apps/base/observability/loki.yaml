# OpenAether 0.5.0 Sprint C — Loki HA (stockage logs)
# ──────────────────────────────────────────────────────────────────
# Mode: SingleBinary x2 (simple to operate, HA through Loki replication).
# Backend: S3 (MinIO en local, S3 natif en cloud via overlay).
# replication_factor=2: every chunk is replicated across both instances.
#
# Destination S3 : ExternalSecret loki-s3-credentials (externalsecret-loki.yaml),
# fed from `secret/observability/loki-s3` in OpenBao. Credentials AND location
# (endpoint + bucket) travel through that Secret, injected with `valuesFrom`:
# moving from the internal MinIO to the provider's S3 therefore requires NO
# code change at all, only a reseed. The destination is deliberately distinct
# from `backup/s3-primary` — Loki has no business writing into the
# bucket des sauvegardes.
#
# schemaConfig v13 (tsdb): the recommended schema since Loki 2.8+.
# auth_enabled=false: no multi-tenant auth (private cluster).
---
apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: grafana
  namespace: services-observability
  labels:
    openaether.io/managed: "true"
    openaether.io/sprint: "0.5.0-sprintC"
    openaether.io/component: loki
spec:
  url: https://grafana.github.io/helm-charts
  interval: 1h
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: loki
  namespace: services-observability
  labels:
    openaether.io/managed: "true"
    openaether.io/sprint: "0.5.0-sprintC"
    openaether.io/component: loki
spec:
  interval: 1h
  chart:
    spec:
      chart: loki
      version: "6.25.0"
      sourceRef:
        kind: HelmRepository
        name: grafana
        namespace: services-observability
      interval: 1h
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  valuesFrom:
    - kind: Secret
      name: loki-s3-credentials
      valuesKey: accessKey
      targetPath: loki.storage.s3.accessKeyId
    - kind: Secret
      name: loki-s3-credentials
      valuesKey: secretKey
      targetPath: loki.storage.s3.secretAccessKey
    # endpoint and bucket come from the SAME Secret: the chart only accepts
    # literal strings, and this brick cannot be substituted by Flux (the Grafana
    # dashboards contain ${datasource} placeholders that would be blanked).
    # Result: moving from the internal MinIO to the provider's S3 ONLY requires
    # reseeding `secret/observability/loki-s3` — no code change.
    - kind: Secret
      name: loki-s3-credentials
      valuesKey: endpoint
      targetPath: loki.storage.s3.endpoint
    - kind: Secret
      name: loki-s3-credentials
      valuesKey: bucket
      targetPath: loki.storage.bucketNames.chunks
    - kind: Secret
      name: loki-s3-credentials
      valuesKey: bucket
      targetPath: loki.storage.bucketNames.ruler
    - kind: Secret
      name: loki-s3-credentials
      valuesKey: bucket
      targetPath: loki.storage.bucketNames.admin
  values:
    deploymentMode: SingleBinary
    singleBinary:
      replicas: 2
      securityContext:
        fsGroup: 10001
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: loki
                    app.kubernetes.io/component: single-binary
                topologyKey: kubernetes.io/hostname
      persistence:
        enabled: true
        storageClass: local-path
        size: 10Gi
      resources:
        requests:
          cpu: 100m
          memory: 256Mi
        limits:
          cpu: 500m
          memory: 1Gi
    loki:
      auth_enabled: false
      commonConfig:
        replication_factor: 2
      storage:
        type: s3
        # bucketNames and endpoint are OVERWRITTEN by the `valuesFrom` above,
        # from the loki-s3-credentials Secret. These values only act as
        # documentation du format attendu.
        bucketNames:
          chunks: loki
          ruler: loki
          admin: loki
        s3:
          endpoint: http://minio.foundation-storage:9000
          region: us-east-1
          s3ForcePathStyle: true
          insecure: true
      schemaConfig:
        configs:
          - from: "2024-01-01"
            store: tsdb
            object_store: s3
            schema: v13
            index:
              prefix: loki_index_
              period: 24h
      limits_config:
        retention_period: 30d
        reject_old_samples: true
        reject_old_samples_max_age: 168h
        ingestion_rate_mb: 10
        ingestion_burst_size_mb: 20
    # No bundled MinIO (we use our own instance).
    minio:
      enabled: false
    # memcached caches disabled: the Loki chart sizes them for production
    # (chunksCache ~9.6Gi + resultsCache ~1.2Gi of memory requests). The
    # chunksCache (8Gi alloc) fits on NO DEV-S worker (~3.3Gi allocatable) →
    # permanently Pending, and the memory pressure also blocks vminsert
    # scheduling. In SingleBinary dev (low log volume) the cache brings nothing
    # essential. To re-enable and size if the
    # workers grossissent (cf. sizing nodes / overlay cloud).
    chunksCache:
      enabled: false
    resultsCache:
      enabled: false
    # No gateway/ingress (internal access only, through the ClusterIP service).
    gateway:
      enabled: false
    # Disable the distributed components that SingleBinary does not need.
    backend:
      replicas: 0
    read:
      replicas: 0
    write:
      replicas: 0
    monitoring:
      serviceMonitor:
        enabled: false
      selfMonitoring:
        enabled: false
      lokiCanary:
        enabled: false
