apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: platform-critical
  labels:
    openaether.io/managed: "true"
    openaether.io/sprint: "0.5.0-sprint0"
    openaether.io/component: platform
description: "OpenAether 0.5.0 platform-critical: OpenBao (root + workload),
  cert-manager, ESO, CSI driver, Flux, Kyverno."
globalDefault: false
preemptionPolicy: Never
value: 100000000
