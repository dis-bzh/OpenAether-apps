apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - standard-install.yaml  # vendored from v1.1.0 — avoids a remote fetch (Docker DNS)

labels:
  - includeSelectors: false
    pairs:
      openaether.io/managed: "true"
      openaether.io/sprint: "0.5.0-sprintB"
      openaether.io/component: "gateway-api"
