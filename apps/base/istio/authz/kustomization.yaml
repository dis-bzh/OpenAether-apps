apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Istio resources requiring the security.istio.io CRDs (installed by the istio-base HelmRelease)
# Applied AFTER the 'istio' Kustomization is Ready (wait: true)
resources:
  - peerauthentication-strict.yaml
  - authorizationpolicy-default-deny.yaml
  - authorizationpolicy-explicit-allows.yaml
