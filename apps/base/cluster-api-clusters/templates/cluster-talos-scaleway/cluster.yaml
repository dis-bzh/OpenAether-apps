# ⚠️ SCAFFOLD NON INSTANCIÉ — cluster client Talos sur Scaleway via CAPI.
# apiVersions validées contre les providers épinglés (2026-07-25) :
#   - CAPS v0.2.1  → infrastructure.cluster.x-k8s.io/v1alpha2 (v1alpha1 déprécié)
#   - CACPPT v0.5.13 → controlplane.cluster.x-k8s.io/v1alpha3
#   - CABPT  v0.6.12 → bootstrap.cluster.x-k8s.io/v1alpha3
# Reste à exercer sur un management réel (première instanciation).
#
# Prérequis : Secret `scaleway-capi-credentials` dans capi-clusters
# (SCW_ACCESS_KEY/SCW_SECRET_KEY/SCW_DEFAULT_PROJECT_ID — via ESO, clé
# OpenBao secret/capi/scaleway, à seeder par l'opérateur).
#
# Variables (postBuild.substitute, cf. apps/clusters/README.md) :
#   ${CLUSTER_NAME}      — nom du cluster enfant (ex: edge-1)
#   ${K8S_VERSION:=v1.31.4}  ${TALOS_VERSION:=v1.9}
#   ${CP_REPLICAS:=1}    ${WORKER_REPLICAS:=1}
#   ${SCW_PROJECT_ID}    ${SCW_REGION:=fr-par}
#   ${SCW_INSTANCE_TYPE:=DEV1-L}
#   ${SCW_IMAGE_NAME}    — nom de l'image Talos (talos-image/ côté infra, même schematic)
#
# Même philosophie que le parent : Talos partout, socle Cilium+Flux injecté
# ensuite par le management (cf. apps/clusters/README.md).
---
apiVersion: cluster.x-k8s.io/v1beta2
kind: Cluster
metadata:
  name: ${CLUSTER_NAME}
  namespace: capi-clusters
  labels:
    openaether.io/managed: "true"
    openaether.io/component: capi-clusters
    cluster.x-k8s.io/cluster-name: ${CLUSTER_NAME}
spec:
  clusterNetwork:
    pods:
      cidrBlocks: ["10.244.0.0/16"]
    services:
      cidrBlocks: ["10.96.0.0/12"]
  # CAPI v1.13 → v1beta2 : refs {apiGroup, kind, name} (plus d'apiVersion/namespace).
  controlPlaneRef:
    apiGroup: controlplane.cluster.x-k8s.io
    kind: TalosControlPlane
    name: ${CLUSTER_NAME}-cp
  infrastructureRef:
    apiGroup: infrastructure.cluster.x-k8s.io
    kind: ScalewayCluster
    name: ${CLUSTER_NAME}
---
apiVersion: infrastructure.cluster.x-k8s.io/v1alpha2
kind: ScalewayCluster
metadata:
  name: ${CLUSTER_NAME}
  namespace: capi-clusters
spec:
  projectID: ${SCW_PROJECT_ID}
  region: ${SCW_REGION:=fr-par}
  # Secret des creds Scaleway (clés SCW_ACCESS_KEY / SCW_SECRET_KEY,
  # "all other keys ignored" — docs CAPS) — cf. en-tête.
  scalewaySecretName: scaleway-capi-credentials
  # Épinglé sur la zone de l'image Talos (les images instance sont PAR ZONE :
  # sans failureDomains, CAPS répartit sur toutes les zones de la région et
  # les machines hors zone ne trouvent pas l'image).
  failureDomains:
    - ${SCW_ZONE:=fr-par-1}
  # Le LB control-plane est TOUJOURS créé par CAPS (non désactivable).
  # Optionnel : network.privateNetwork / publicGateways — reprendre les choix
  # de modules/providers/scw côté infra pour un enfant durci.
---
apiVersion: controlplane.cluster.x-k8s.io/v1alpha3
kind: TalosControlPlane
metadata:
  name: ${CLUSTER_NAME}-cp
  namespace: capi-clusters
spec:
  replicas: ${CP_REPLICAS:=1}
  version: ${K8S_VERSION:=v1.31.4}
  infrastructureTemplate:
    apiVersion: infrastructure.cluster.x-k8s.io/v1alpha2
    kind: ScalewayMachineTemplate
    name: ${CLUSTER_NAME}-cp
  controlPlaneConfig:
    controlplane:
      generateType: controlplane
      talosVersion: ${TALOS_VERSION:=v1.9}
      # Le CNI n'est PAS posé ici (pas d'inlineManifests ~MB dans un CR) :
      # Cilium + Flux sont injectés à distance par le management (kubeConfig).
      # strategicPatches et PAS configPatches (JSON6902) : "JSON6902 patches
      # are not supported for multi-document machine configuration" avec les
      # configs Talos récentes (constaté en réel, CABPT v0.6.12 / Talos v1.13).
      strategicPatches:
        - |
          cluster:
            network:
              cni:
                name: none
            proxy:
              disabled: true
---
apiVersion: bootstrap.cluster.x-k8s.io/v1alpha3
kind: TalosConfigTemplate
metadata:
  name: ${CLUSTER_NAME}-workers
  namespace: capi-clusters
spec:
  template:
    spec:
      generateType: join
      talosVersion: ${TALOS_VERSION:=v1.9}
---
apiVersion: cluster.x-k8s.io/v1beta2
kind: MachineDeployment
metadata:
  name: ${CLUSTER_NAME}-workers
  namespace: capi-clusters
spec:
  clusterName: ${CLUSTER_NAME}
  replicas: ${WORKER_REPLICAS:=1}
  selector:
    matchLabels:
      cluster.x-k8s.io/cluster-name: ${CLUSTER_NAME}
  template:
    metadata:
      labels:
        cluster.x-k8s.io/cluster-name: ${CLUSTER_NAME}
    spec:
      clusterName: ${CLUSTER_NAME}
      version: ${K8S_VERSION:=v1.35.3}
      bootstrap:
        configRef:
          apiGroup: bootstrap.cluster.x-k8s.io
          kind: TalosConfigTemplate
          name: ${CLUSTER_NAME}-workers
      infrastructureRef:
        apiGroup: infrastructure.cluster.x-k8s.io
        kind: ScalewayMachineTemplate
        name: ${CLUSTER_NAME}-workers
---
apiVersion: infrastructure.cluster.x-k8s.io/v1alpha2
kind: ScalewayMachineTemplate
metadata:
  name: ${CLUSTER_NAME}-cp
  namespace: capi-clusters
spec:
  template:
    spec:
      commercialType: ${SCW_INSTANCE_TYPE:=DEV1-L}
      # Exactement UN de id|name|label (CAPS v1alpha2, champs immuables).
      image:
        name: ${SCW_IMAGE_NAME}
      # IPv4 publique : sans réseau privé/gateway configurés, c'est l'egress
      # des nœuds (pull images, GitHub pour le Flux enfant).
      publicNetwork:
        enableIPv4: true
---
apiVersion: infrastructure.cluster.x-k8s.io/v1alpha2
kind: ScalewayMachineTemplate
metadata:
  name: ${CLUSTER_NAME}-workers
  namespace: capi-clusters
spec:
  template:
    spec:
      commercialType: ${SCW_INSTANCE_TYPE:=DEV1-L}
      image:
        name: ${SCW_IMAGE_NAME}
      publicNetwork:
        enableIPv4: true
