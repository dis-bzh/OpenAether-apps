# OpenAether — HTTPRoute Longhorn UI → openaether-gateway
# parentRef: openaether-gateway (Istio, ns services-gateway, from: All).
# Longhorn UI (longhorn-frontend:80) — distributed storage management.
#
# ⚠️ AUTH: the Longhorn UI has NO native authN. Protection relies for now on
# l'instant sur l'ACL du LB (admin_ip, niveau infra). Durcissement futur :
# basicAuth through the Gateway or Zitadel OIDC. See the admin access README.
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn
  namespace: longhorn-system
  labels:
    openaether.io/managed: "true"
    openaether.io/component: storage
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: openaether-gateway
      namespace: services-gateway
  hostnames:
    - "longhorn.openaether.local"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: longhorn-frontend
          port: 80
