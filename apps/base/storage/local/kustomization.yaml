apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Local storage stub — remplace apps/base/storage en local.
# Longhorn requiert l'extension Talos iscsi-tools, absente de l'image Docker.
# Locally: local-path-provisioner is the only StorageClass (deployed through
# apps/flux/local/00b-local-storage.yaml → platform-local-storage).
# This directory is intentionally empty to prevent any Longhorn deployment.
resources: []
