apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: openbao-workload
spec:
  vault:
    path: pki/workload/sign/openaether-leaf
    server: http://openbao.foundation-vault.svc.cluster.local:8200
    auth:
      kubernetes:
        mountPath: /v1/auth/kubernetes
        role: cert-manager-issuer
        serviceAccountRef:
          name: cert-manager
